spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8888
  security:
    user:
      name: kv
      password: kv
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false
management:
  endpoints:
    web:
      exposure:
        include: '*'